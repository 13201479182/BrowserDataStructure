
    概念
        单向链表节点: 每个节点中仅存储着下一个节点的指针
        双向链表节点: 每个节点中存储着上一个节点和下一个节点的指针

        由以上节点线性存储起来的结构,统称为单向或者双向链表

    特点
        读取 O(n)
        插入和删除 O(1)

    应用:
        应用案例(git commit)
        最适合做任务管理器
            1. 读取不频繁
            2. 需要频繁的将任务插入至具体的任务之前或者之后
            3. 需要频繁的删除任务